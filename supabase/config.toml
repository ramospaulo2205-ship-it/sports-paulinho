project_id = "flltoxywqffocnlbottb"

# verify_jwt fica false de propósito: a autorização do scraper é feita
# internamente via header x-cron-secret (só o pg_cron conhece o segredo).
[functions.scrape-bookmakers]
verify_jwt = false